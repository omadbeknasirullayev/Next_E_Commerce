
const app = Vue.createApp({
    data() {
        return {
            card_info: infos,
            brendsName: brendsName,
            shops: shops,
        }
    },

    methods: {},

    mounted() {
       
    }
})

app.mount('#root')